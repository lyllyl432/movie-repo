
//get the url api of the tmdb movie api
//create a key for the authorization of the application
//fetch the popular data using the fetch API (it combines the base url and api key)
//add the function on window event listener to fire if the DOM sync done
//display the data or item to the carousel DOM
//add another js file for carousel object

//fetch the highest rating using the fetch API
//display highest rating data to the DOM
//add event listener to the movie items in carousel and to the non carousel DOM
//create an Object of Handling the Storage
//when click it will display to the other page


//make a function on input to appear on DOM
//use search in API to search movies with same keywords


//if i click the check button it will update the localStorage as a watched movies



    //     dateLink.addEventListener("click", ()=>{
    //         if(showDate !== "enabled"){
    //             this.showDateFilter(movieContainer);
    //         } {
                
    //         }
    //     })
    //     popularityLink.addEventListener("click",()=>{
    //         if(showPopular !== "enabled"){
    //             this.showPopularFilter(movieContainer);
    //         }
    //         console.log("hello world");
    //     })
    //     if(showDate === "enabled"){
    //         this.showDateFilter(movieContainer);
    //     }
    // }
    // showDateFilter(movieContainer){
    //    for(let container of movieContainer){
    //     if(container.classList.contains("sort-by-date")){
    //         container.classList.add("show");
    //         localStorage.setItem("date-filter", "enabled");
    //     }
    //    }
    // }
    // showPopularFilter(movieContainer){
    //     for(let container of movieContainer){
    //         if(container.classList.contains("sort-by-popular")){
    //             container.classList.add("show");
    //             localStorage.setItem("popularity-filter", "enabled");
    //         }
    //        }
    //        localStorage.setItem("date-filter","disabled");
    // }
    // hideFilter(movieContainer){
    //     for(let container of movieContainer){
    //         if(container.classList.contains("sort-by-date")){
    //             container.classList.remove("show");
    //             localStorage.setItem("show-movies", "disabled");
    //         }
    //        }
    // }